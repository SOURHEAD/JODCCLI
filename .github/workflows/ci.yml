name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Docker image
        run: docker build -t my-organize-app .

      - name: Run Docker container
        run: docker run -d -p 2222:22 --name organize-app my-organize-app

      - name: Wait for Docker container to start
        run: sleep 10

      - name: Test HTTP response code
        run: docker exec organize-app curl -o /dev/null -s -w "%{http_code}" http://localhost:80/organize | grep "200"

      - name: Clean up
        run: docker stop organize-app && docker rm organize-app
